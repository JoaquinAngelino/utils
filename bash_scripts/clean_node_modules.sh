#!/bin/bash

# Remove node_modules from all directories at depth 1

for dir in */; do
    if [ -d "${dir}node_modules" ]; then
        echo "Removing node_modules from: $dir"
        rm -rf "${dir}node_modules"
    fi
done

echo "Done!"
